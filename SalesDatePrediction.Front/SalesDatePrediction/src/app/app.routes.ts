import { Routes } from '@angular/router';
import { CustomerComponent } from './component/customer-view/customer-view.component';

export const routes: Routes = [
  { path: '', component: CustomerComponent },
];